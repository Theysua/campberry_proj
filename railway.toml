[build]
builder = "DOCKERFILE"
dockerfilePath = "campberry_backend/Dockerfile"
watchPatterns = ["campberry_backend/**"]

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
